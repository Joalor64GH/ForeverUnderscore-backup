function onStepHit(curStep)
{
    switch (curStep)
    {
        case 60, 444, 524, 828:
            dad.playAnim('singUP-alt', true);
            dad.specialAnim = true;
            dad.heyTimer = 0.6;
    }
}